import { SidePanelShell } from "@/components/perceptual-web/SidePanelShell";

export const metadata = {
  title: "Perceptual Web — Side Panel",
  description: "Accessible regenerated page view",
};

/** Side panel shell — extension entry + dev preview */
export default function SidePanelPage() {
  return <SidePanelShell />;
}
